# Server Configuration
PORT=3000
HOST=localhost

# For production deployment, set HOST to 0.0.0.0 to accept connections from any IP
# PORT will typically be set by your cloud platform (e.g., Railway, Render)
# Example for production:
# HOST=0.0.0.0
# PORT=3000
